---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "scm_globalprotect_match_list Resource - scm"
subcategory: ""
description: |-
  GlobalprotectMatchList resource
---

# scm_globalprotect_match_list (Resource)

GlobalprotectMatchList resource

## Example Usage

```terraform
resource "scm_globalprotect_match_list" "globalprotect_match_list" {
  name             = "test-globalprotect-list"
  description      = "GlobalProtect match list for monitoring VPN connection events and remote access activities"
  folder           = "ngfw-shared"
  filter           = "All Logs"
  send_syslog      = ["test-syslog"]
  send_http        = ["some-http-profile"]
  send_snmptrap    = ["snmp_test"]
  send_email       = ["test-email"]
  quarantine       = false
  send_to_panorama = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the globalprotect match list entry

### Optional

- `description` (String) Description of the globalprotect match list entry
- `device` (String) The device in which the resource is defined

> ℹ️ **Note:** You must specify exactly one of `device`, `folder`, and `snippet`.
- `filter` (String) Filter of the globalprotect match list entry
- `folder` (String) The folder in which the resource is defined

> ℹ️ **Note:** You must specify exactly one of `device`, `folder`, and `snippet`.
- `quarantine` (Boolean) Quarantine Flag of the globalprotect match list entry
- `send_email` (List of String) Send Email List of the globalprotect match list entry
- `send_http` (List of String) Send HTTP List of the globalprotect match list entry
- `send_snmptrap` (List of String) Send SNMP Trap List of the globalprotect match list entry
- `send_syslog` (List of String) Send Sys log List of the globalprotect match list entry
- `send_to_panorama` (Boolean) Send to Panorama Flag of the globalprotect match list entry
- `snippet` (String) The snippet in which the resource is defined

> ℹ️ **Note:** You must specify exactly one of `device`, `folder`, and `snippet`.

### Read-Only

- `id` (String) UUID of the resource
- `tfid` (String) The Terraform ID.


## Import

The following command can be used to import a resource not managed by Terraform:

```bash
terraform import scm_globalprotect_match_list.example folder:::id
```

or

```bash
terraform import scm_globalprotect_match_list.example :snippet::id
```

or

```bash
terraform import scm_globalprotect_match_list.example ::device:id
```

**Note:** Please provide just one of folder, snippet, or device for the import command.

